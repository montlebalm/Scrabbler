import unittest
from solver import solver

class Test(unittest.TestCase):
		
	#def test_word_value(self):
		#a = anagram.Anagram()
		#word = a.get_words('abrez')
		#value = a.get_word_value(word)
		#print word + " is worth " + str(value) + " points"